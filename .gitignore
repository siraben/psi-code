build/
result
result-*

# Local environment and scratch files. .env.local carries
# ANTHROPIC_API_KEY; keep it out of commits by default.
.env.local
.env
apr*.jsonl
pi-mono/

# Per-user bench target config (hostnames, ssh jump hops, binary paths).
# Template lives at tests/bench.targets.example.json.
tests/bench.targets.json

# Haiku VM artifacts — large and reproducible.
haiku/downloads/
haiku/guest.iso
haiku/disk.qcow2
haiku/guest/stage/

# 9front VM artifacts — large and reproducible.
9front/downloads/
9front/disk.qcow2
9front/tools/__pycache__/
