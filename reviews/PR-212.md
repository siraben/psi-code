# Review — PR #212: Fix out-of-order overlay focus restoration

**Branch:** `siraben/overlay-focus-cleanup` @ `2796c8f2` · **Base:** `master` · **Date:** 2026-08-22
**Mode:** Standard
**Method:** dual-model (Claude subagents + Codex CLI), parallel discovery, full cross-validation (each model's findings adjudicated by the other). Reference: `~/pi-mono` @ `c49906ec`.

## Parity verdict: **ACHIEVED**

Both models independently ruled ACHIEVED with zero findings.

## Summary of surviving findings

_No surviving findings._

## Author intent

> - retarget descendant overlay restoration links before permanently removing an overlay
- prevent later focus restoration and key dispatch from targeting an unmounted overlay
- cover two- and three-level overlays removed in multiple out-of-order sequences

## Claude track (discovery)

```
PARITY_VERDICT: ACHIEVED — psi's retarget_overlay_pre_focus (lua/psi/tui_app.lua:83-89) is a faithful translation of pi-mono's retargetOverlayPreFocus (/home/siraben/pi-mono/packages/tui/src/tui.ts:523-529): iterate remaining stack entries, skip the removed one, rewrite any pre_focus == removed.component to removed.pre_focus. Invoked before stack removal in psi's sole removal path (handle.hide, tui_app.lua:220-221), matching pi-mono's hide-handle ordering (tui.ts:571-572) and covering the role of pi-mono's other call site hideOverlay (tui.ts:649-650) since psi has only one table.remove site. All downstream pre_focus consumers mirror pi-mono's.
FINDINGS: none
PI_GAPS:
- pi-mono's companion clearOverlayFocusRestoreFor(entry) and the broader overlayFocusRestore eligible/blocked/resume machinery (tui.ts:361,435-467,487-510,862-888 incl cycle-safe isOverlayFocusAncestor walk tui.ts:512-521) have no psi counterpart; behaviors exercised by pi-mono tests like automatic overlay re-focus after a non-overlay component steals input (overlay-non-capturing.test.ts:381,472) remain outside psi's simpler App focus model — pre-existing architectural simplification untouched by this diff.
```

## Codex track (discovery)

```
PARITY_VERDICT: ACHIEVED — pi-mono captures the active component as `preFocus` when creating an overlay (`packages/tui/src/tui.ts:549-557`). It retargets surviving restoration links before removing the overlay and restoring focus (`packages/tui/src/tui.ts:523-528`, `packages/tui/src/tui.ts:567-576`). Its regression case confirms input returns to the editor after child-before-parent removal (`packages/tui/test/overlay-non-capturing.test.ts:226-249`), which this PR matches.
FINDINGS: none
PI_GAPS: none
```

## Cross-validation record

```
(no findings to validate)
```


## Limitations

- Static review only; CI (Lua suite, build matrix, PTY tests) treated as green per Forgejo Actions runs. No manual TUI interaction testing was performed.
- pi-mono parity claims cite upstream TypeScript sources read at review time (`c49906ec`); runtime Node/ICU behavior probed only where noted (#220).
- Diff scope: single commit vs origin/master.
