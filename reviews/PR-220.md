# Review — PR #220: fix(tui): treat Indic conjuncts as atomic graphemes

**Branch:** `siraben/tui-indic-graphemes` @ `1f2ff5e9` · **Base:** `master` · **Date:** 2026-08-22
**Mode:** Critical (C-Lua boundary)
**Method:** dual-model (Claude subagents + Codex CLI), parallel discovery, full cross-validation (each model's findings adjudicated by the other). Reference: `~/pi-mono` @ `c49906ec`.

## Parity verdict: **PARTIAL**

Empirically verified against ICU 78.3 for the claimed corpus; ZWNJ re-join divergence VALID in both implementations.

## Summary of surviving findings

| Sev | Location | Category | Found | Validated | Finding |
|---|---|---|---|---|---|
| should_fix | `lua/psi/tui_text.lua:360-363 + src/lua/vm.c:1561-1578` | bug | codex | claude | ZWNJ does not permanently invalidate the Indic chain; re-joins where Intl.Segmenter breaks |

## Author intent

> - implement Unicode 17 Indic conjunct linker/consonant segmentation in both the C terminal primitives and Lua editor fallback
- keep virama and virama+ZWJ conjuncts atomic for movement, deletion, clipping, and wrapping while preserving pi-mono cell accounting
- treat ZWNJ and bare Indic ZWJ as boundaries, matching `Intl.Segmenter`
- add cross-script editing regressions and deterministic C/Lua pari

## Claude track (discovery)

```
PARITY_VERDICT: ACHIEVED — pi-mono segments graphemes with runtime Intl.Segmenter (packages/tui/src/utils.ts:4) and moves/deletes on those boundaries (components/editor.ts:18,362; input.ts:6,9); ICU GB9c makes conjuncts, virama+ZWJ conjuncts, ZWNJ/bare-ZWJ boundaries exactly what psi reimplements. Empirically probed node ICU 78 on all ten test conjuncts + ZWJ/ZWNJ/chain controls — each matches psi PR-head behavior incl cell accounting (graphemeWidth utils.ts:174-235 = psi join-width accumulation). C host (src/lua/vm.c:1529-1596) and Lua fallback (lua/psi/tui_text.lua:335-385) implement identical state machine; tables mechanically in sync (20 linkers, 76 ranges). Residual divergences only in edge sequences beyond PR claim.
FINDINGS: none
PI_GAPS:
- Matra/vowel-sign attachment: C-linker-C + spacing mark (स्ते): Intl one cluster (GB9a SpacingMark) vs psi स्त|े because src/core/wcwidth.c has no Devanagari zero-width entries (falls to width 1; tui_text.lua:328-333 / vm.c:1521-1527 reject matras). Pre-existing pattern diff narrows (4 clusters -> 2); movement boundaries still differ though total width agrees.
- Non-ZWJ InCB extenders between linker and consonant (nukta U+093C, anusvara U+0902): ICU keeps one cluster vs psi splits; same wcwidth-table reason.
- Re-linking after ZWNJ: क ् ZWNJ ् ष — Intl breaks before final consonant but psi re-arms indic_linker_pending on second linker (tui_text.lua:357-359; vm.c:1558-1560) and rejoins. Newly introduced edge-case divergence; fix: latch a per-cluster "ZWNJ seen" flag permanently disabling GB9c joining, mirroring ICU.
- Consonant + bare ZWJ + spacing mark (क‍ि): ICU joins via GB9a and pre-diff psi joined via saw_zwj; diff suppresses saw_zwj for Indic-initial clusters (tui_text.lua:366-371; vm.c:1567-1573) so now splits. Only reachable on malformed orthography; intentional per PR body but not Intl-exact for this neighbor sequence.
```

## Codex track (discovery)

```
PARITY_VERDICT: PARTIAL — pi-mono delegates grapheme boundaries to a shared Intl.Segmenter (`packages/tui/src/utils.ts:3-12`) and uses its segments for deletion and cursor movement (`packages/tui/src/components/editor.ts:1288-1302`, `1815-1842`). Its width and clipping paths iterate the same segments while counting Indic spacing (`packages/tui/src/utils.ts:210-234`, `279-283`, `1226-1239`); this PR matches ordinary virama and virama+ZWJ conjuncts but mishandles one ZWNJ ordering.
FINDINGS:
- title: ZWNJ does not permanently invalidate the current Indic chain
  path: lua/psi/tui_text.lua line: 360-363
  severity: should_fix
  category: bug
  reason_flagged: The U+200C branch clears only the pending linker, allowing a subsequent virama to re-enable a join that pi-mono’s Intl grapheme segmenter does not make (`packages/tui/src/utils.ts:3-12`).
  body: After U+200C is consumed, `cluster_is_indic` remains true, so U+0915 U+200C U+094D U+0915 is folded into one cluster when the later linker sets `indic_linker_pending` again; right-arrow and backspace consequently skip or delete the final consonant, and the mirrored C state machine at `src/lua/vm.c:1561-1578` gives clipping and wrapping the same incorrect boundary; invalidate Indic-chain eligibility after ZWNJ in both implementations and add a ZWNJ-before-linker regression.
PI_GAPS:
- ZWNJ followed by a linker can still reconnect to the initial consonant in psi, unlike the shared Intl segmentation used throughout pi-mono’s editor (`packages/tui/src/components/editor.ts:360-363`, `1288-1302`, `1815-1842`) and rendering utilities (`packages/tui/src/utils.ts:279-283`, `1226-1239`).
```

## Cross-validation record

```
FINDING 1: ZWNJ does not permanently invalidate current Indic chain — VALID — Hand-trace of git show 1f2ff5e9:lua/psi/tui_text.lua :337-375: U+0915 sets cluster_is_indic=true (:339); U+200C hits elseif next_cp==0x200c branch (:360-363) clearing indic_linker_pending/saw_zwj but NEVER cluster_is_indic; U+094D re-arms via cluster_is_indic and is_indic_linker (:357); U+0915 folded by conjunct branch (:372) — all bytes one cluster; next_grapheme_index/backspace (:397-407) and mirrored C machine (vm.c ZWNJ else-if :1561, linker/consonant fold :1572-1576) skip/delete final consonant. Ground truth empirical: Node v26.7.0 ICU 78.3 Intl.Segmenter grapheme on U+0915 U+200C U+094D U+0915 yields two segments "क‌्"|"क" — ZWNJ attaches to preceding consonant via GB9 (GCB=Extend), U+094D Extend stays in segment, GB9c does NOT re-join final U+0915 (ZWNJ not InCB=Extend/Linker; GB9a irrelevant); pi consumes exactly this segmentation (utils.ts:3, editor.ts:1288-1302 backspace, 1815-1842 arrows) — psi folds where pi keeps boundary. PR regression test covers only reverse order virama-before-ZWNJ (tests/lua/tui/indic_grapheme_parity.lua:410-416) so green CI consistent. should_fix correct: real divergence in PR core feature across both impls, narrow pattern, no crash/corruption — not blocking, more than nit.
```


## Limitations

- Static review only; CI (Lua suite, build matrix, PTY tests) treated as green per Forgejo Actions runs. No manual TUI interaction testing was performed.
- pi-mono parity claims cite upstream TypeScript sources read at review time (`c49906ec`); runtime Node/ICU behavior probed only where noted (#220).
- Diff scope: single commit vs origin/master.
