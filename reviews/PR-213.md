# Review — PR #213: Clean up external-editor temporary files

**Branch:** `siraben/external-editor-cleanup` @ `b2ee3871` · **Base:** `master` · **Date:** 2026-08-22
**Mode:** Standard
**Method:** dual-model (Claude subagents + Codex CLI), parallel discovery, full cross-validation (each model's findings adjudicated by the other). Reference: `~/pi-mono` @ `c49906ec`.

## Parity verdict: **ACHIEVED**

Both models independently ruled ACHIEVED with zero findings.

## Summary of surviving findings

_No surviving findings._

## Author intent

> - remove external-editor temporary files after success, launch failure, nonzero exit, read failure, and write failure
- preserve the existing prompt when the edited file cannot be read
- strip a leading UTF-8 BOM and retain pi-mono’s single trailing-newline behavior
- keep cleanup best-effort and guarantee it across unexpected Lua errors with a finally-style transaction

## Claude track (discovery)

```
PARITY_VERDICT: ACHIEVED — psi now mirrors pi-mono packages/coding-agent/src/modes/interactive/external-editor.ts: cleanup on every path via xpcall + pcall(remove_file) (tui_runtime.lua:3650-3652 vs try/finally rmSync external-editor.ts:17,39-45); launch failure and nonzero exit both failed (external-editor.ts:30-36); success strips BOM + single trailing newline (external-editor.ts:38, stripBom utils/text.ts:3-9 vs chat.strip_utf8_bom + gsub tui_runtime.lua:3646); read failure preserves prompt like pi's caller (interactive-mode.ts:4214-4225). Remaining diffs (single mkstemp file vs per-edit mkdtemp dir holding prompt.md; psi adds status-line failure messages) are architecture-driven per PR policy split and docs/port-status.md Ctrl-G entry.
FINDINGS: none
PI_GAPS:
- pi removes the whole per-edit temp directory recursively (mkdtempSync "pi-editor-" + prompt.md, external-editor.ts:15-16; rmSync recursive force, :41), so editor sidecar files (vim swap/undo beside prompt.md) are cleaned too; psi unlinks only the one mkstemp file (os.remove tui_runtime.lua:3610,3651), so siblings like /tmp/.psi-editor-XXXXXX.swp survive.
- pi gives editors an extension-bearing filename (prompt.md, :16) as syntax-highlighting hint; psi's psi.tempfile_path("psi-editor-") is extensionless (src/lua/vm.c:2720-2773) — pre-existing, unchanged by this diff.
```

## Codex track (discovery)

```
PARITY_VERDICT: ACHIEVED — pi-mono creates the temporary editor workspace before writing the draft and encloses write, launch, exit handling, and readback in a cleanup transaction (`packages/coding-agent/src/modes/interactive/external-editor.ts:15-45`). It accepts edits only after a zero exit and strips the leading BOM plus exactly one trailing newline (`packages/coding-agent/src/modes/interactive/external-editor.ts:34-38`; `packages/coding-agent/src/utils/text.ts:1-8`). Its caller replaces the prompt only on completion, preserving the existing draft on failure, and always restores the UI (`packages/coding-agent/src/modes/interactive/interactive-mode.ts:4210-4225`).
FINDINGS: none
PI_GAPS: none
```

## Cross-validation record

```
(no findings to validate)
```


## Limitations

- Static review only; CI (Lua suite, build matrix, PTY tests) treated as green per Forgejo Actions runs. No manual TUI interaction testing was performed.
- pi-mono parity claims cite upstream TypeScript sources read at review time (`c49906ec`); runtime Node/ICU behavior probed only where noted (#220).
- Diff scope: single commit vs origin/master.
