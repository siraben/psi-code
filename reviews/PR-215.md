# Review — PR #215: Add TUI editor undo and kill ring

**Branch:** `siraben/editor-undo-kill-ring` @ `a3d6ef9d` · **Base:** `master` · **Date:** 2026-08-22
**Mode:** Critical (C-Lua boundary: src/lua/vm.c)
**Method:** dual-model (Claude subagents + Codex CLI), parallel discovery, full cross-validation (each model's findings adjudicated by the other). Reference: `~/pi-mono` @ `c49906ec`.

## Parity verdict: **PARTIAL**

Claude initially ACHIEVED; four codex should_fix findings all VALID after Claude cross-validation.

## Summary of surviving findings

| Sev | Location | Category | Found | Validated | Finding |
|---|---|---|---|---|---|
| should_fix | `src/lua/vm.c:1033-1037` | api_boundary | codex | claude | Legacy Ctrl-Minus (raw 0x1f) cannot trigger undo |
| should_fix | `lua/psi/tui_runtime.lua:3435` | bug | codex | claude | Unicode whitespace breaks fish-style undo grouping |
| should_fix | `lua/psi/tui_runtime.lua:1175-1201` | bug | codex | claude | Reverse-search recall has no undo snapshot |
| should_fix | `lua/psi/tui_runtime.lua:4506-4529` | bug | codex+claude | claude | Rejected submit clears undo history before restoring input |

## Author intent

> - add fish-style grouped editor undo with snapshots for typing, deletion, paste, completion, history recall, clear, Vim insertion, and external-editor replacement
- add an Emacs-style kill ring for word and line kills, including directional accumulation, yank, and yank-pop cycling
- add configurable Ctrl-Y, Alt-Y, and Ctrl-Minus bindings plus real terminal decoding for Alt-Y and Kitty Ctrl-Minus
-

## Claude track (discovery)

```
PARITY_VERDICT: ACHIEVED — Fish-style grouping matches pi exactly: whitespace-or-first-char snapshot capture with lastAction="type-word" coalescing (pi editor.ts:1108-1121) maps to insert_text rule (tui_runtime.lua:3435-3442); atomic paste/newline/completion/history-entry/clear/external-editor/submit-clear one-to-one counterparts (editor.ts:1168-1173,1236-1259,679-701,427-457,1022-1034 used by clearEditor interactive-mode.ts:4232-4235 + external-editor :4224, submitValue clear editor.ts:1272-1282). Kill-ring push/prepend/append/accumulate match kill-ring.ts:19-28 incl was_kill capture and "\n" kills on line joins (editor.ts:1533-1615,1620-1685 vs tui_runtime.lua:3475-3615); yank/yank-pop gating, delete-then-rotate-then-insert, end-of-insert cursor placement match editor.ts:1906-1937,1986-2017. Bindings ctrl-y/alt-y/ctrl-- match pi defaults (keybindings.ts:140-142); vm.c decodes ESC-y alt-y (:924-926) and CSI-u [45;5u ctrl-- modifier 5 (:1033-1037) matching pi modValue-1 convention (keys.ts:598-606).
FINDINGS:
- title: Submit clears undo stack before busy-rejected paths that restore editable input
  path: lua/psi/tui_runtime.lua line: 4514-4522
  severity: nit
  category: bug
  reason_flagged: editor_clear_undo runs unconditionally at submit entry; several busy branches then restore state.input=line and return, leaving live prompt whose edit history was discarded.
  body: submit() clears stack at 4514; when state.busy_kind ~= "agent" ("compact"/"btw"/"tree", set at 4179/4239/4324) branch at 4516-4522 restores draft and returns error status; ctrl-- afterwards pops empty stack though prompt editable on screen. User cannot revert last edits after rejected submit during compaction/btw/tree. Fix: move chat.editor_clear_undo to success paths or re-push snapshot when restoring input.
PI_GAPS:
- Queue-restore into editor not undoable in psi: restore_queued_message assigns state.input=combined directly without editor_push_undo (tui_runtime.lua:3796-3804) vs pi routes through editor.setText(combinedText) (interactive-mode.ts:4351-4372) whose diff-check pushes snapshot (editor.ts:1022-1034) — in pi one undo reverts dequeue, in psi ctrl-- pops unrelated older snapshot or no-ops.
- Vim layer normal/visual swallow unbound keys as noop so ctrl-y/alt-y/ctrl-- do nothing outside insert mode (vim_keybindings.lua:238-252 ACTION_NOOP) while pi core handles undo/yank regardless of mode; not part of PR claimed surface (insert-mode works via insert_text).
- sscanf decoder requires exactly [45;5u, drops kitty variants with event-type suffixes like [45;5:1u (vm.c:1034-1036) which pi regex parses (keys.ts:598-606); mirrors pre-existing shift-enter/ctrl-arrow limitation; psi never enables kitty protocol itself.
```

## Codex track (discovery)

```
PARITY_VERDICT: PARTIAL — Core behavior matches pi-mono: directional kills prepend/append at packages/tui/src/components/editor.ts:1533-1676, yank-pop rotates and replaces text at packages/tui/src/components/editor.ts:1903-1975, and undo restores snapshots at packages/tui/src/components/editor.ts:2024-2039. Fish-style grouping is incomplete because pi classifies whitespace through packages/tui/src/components/editor.ts:1111-1120 and packages/tui/src/utils.ts:943-945, while psi uses byte-oriented `%s`. Input parity is also incomplete because pi decodes legacy `\x1f` as Ctrl-Minus at packages/tui/src/keys.ts:1273-1278, whereas this diff only decodes Kitty CSI-u.
FINDINGS:
- title: Legacy Ctrl-Minus cannot trigger undo
  path: src/lua/vm.c line: 1033-1037
  severity: should_fix
  category: api_boundary
  reason_flagged: The new default undo binding remains unreachable for the legacy Ctrl-Minus byte supported by pi-mono at packages/tui/src/keys.ts:1273-1278.
  body: The decoder recognizes only `ESC[45;5u`, while raw `0x1f` falls outside the later Ctrl-A–Ctrl-Z range -> undo silently does nothing in legacy terminals -> map `0x1f` to `ctrl--` before the generic control-byte branch.
- title: Unicode whitespace breaks undo grouping
  path: lua/psi/tui_runtime.lua line: 3435
  severity: should_fix
  category: bug
  reason_flagged: The new grouping test uses Lua `%s`, despite an existing Unicode-space classifier in this file.
  body: `%s` does not classify UTF-8 whitespace such as U+00A0 as whitespace, unlike pi-mono's predicate at packages/tui/src/utils.ts:943-945 -> text separated by such characters collapses into one undo unit -> use the existing `_is_unicode_space` codepoint helper for typed text.
- title: Reverse-search recall has no undo snapshot
  path: lua/psi/tui_runtime.lua line: 1175-1201
  severity: should_fix
  category: bug
  reason_flagged: Ctrl-R is an existing history-recall path omitted from the new undo integration.
  body: Entering reverse search replaces `state.input` without calling `editor_push_undo`, and accepting discards the saved draft -> the next undo restores an older edit instead of the pre-search prompt -> push one snapshot when reverse search first becomes active, matching pi-mono's history-entry checkpoint at packages/tui/src/components/editor.ts:427-456.
- title: Rejected submit clears undo history
  path: lua/psi/tui_runtime.lua line: 4506-4529
  severity: should_fix
  category: bug
  reason_flagged: `editor_clear_undo` runs before branches that reject submission and restore the original input.
  body: A submit attempted during a non-agent busy operation clears the stack and then restores the prompt -> the unchanged prompt loses all undo history -> clear the stack only after committing to a command, queue, or agent submission; pi-mono returns before submission when disabled at packages/tui/src/components/editor.ts:815-829 and clears inside `submitValue` at packages/tui/src/components/editor.ts:1272-1282.
PI_GAPS:
- Legacy-terminal Ctrl-Minus remains unsupported; pi-mono maps raw `\x1f` to `ctrl+-` at packages/tui/src/keys.ts:1273-1278.
- Unicode whitespace does not create pi-style undo boundaries; pi-mono applies its whitespace predicate at packages/tui/src/components/editor.ts:1111-1120.
- Ctrl-R recall does not follow pi-mono's history-recall snapshot semantics at packages/tui/src/components/editor.ts:427-456.
- Rejected submission discards undo history, unlike pi-mono's disabled-submit path at packages/tui/src/components/editor.ts:815-829.
```

## Cross-validation record

```
(no findings to validate)
```


## Limitations

- Static review only; CI (Lua suite, build matrix, PTY tests) treated as green per Forgejo Actions runs. No manual TUI interaction testing was performed.
- pi-mono parity claims cite upstream TypeScript sources read at review time (`c49906ec`); runtime Node/ICU behavior probed only where noted (#220).
- Diff scope: single commit vs origin/master.
