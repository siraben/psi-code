# Review — PR #223: fix(tui): align Lua fallback cell wrapping

**Branch:** `siraben/tui-fallback-wrap-cells` @ `f79946ae` · **Base:** `master` · **Date:** 2026-08-22
**Mode:** Standard
**Method:** dual-model (Claude subagents + Codex CLI), parallel discovery, full cross-validation (each model's findings adjudicated by the other). Reference: `~/pi-mono` @ `c49906ec`.

## Parity verdict: **PARTIAL**

Claude initially ACHIEVED; U+065F width regression in the fallback VALID (diverges from pi-mono AND psi's own native table).

## Summary of surviving findings

| Sev | Location | Category | Found | Validated | Finding |
|---|---|---|---|---|---|
| should_fix | `lua/psi/tui_text.lua:157` | bug | codex | claude | U+065F made zero-width in fallback; pi-mono and psi native count it as one cell |

## Author intent

> With all terminal text host primitives, including `psi.cell_width`, disabled before loading `psi.tui_text`:

## Claude track (discovery)

```
PARITY_VERDICT: ACHIEVED — pi-mono measures base+mark cluster like بّ as 1 cell via base-codepoint eastAsianWidth lookup, zeroes standalone mark graphemes via \p{Mark} (packages/tui/src/utils.ts:40,174-208); Lua fallback now reproduces for all pre-Indic scripts: added ranges (lua/psi/tui_text.lua:148-180) match C host Unicode 17 combining table entry-for-entry below U+0900 (src/core/wcwidth.c:95-121, verified range-by-range); U+303F exclusion (tui_text.lua:197) agrees with host doublewide split (wcwidth.c:439-440, default width 1 at :1608) and EAW=Narrow hence width 1 in pi-mono. Preserve-whitespace fix routing escapes through update_active_escape (tui_text.lua:627, helper :496-499) mirrors AnsiCodeTracker preserving original BEL/ST terminator on close (utils.ts:454-472), close underline+hyperlink at line end / reopen on continuation (:717-726,:689-691), hyperlink state across SGR resets (:666) — identical to native preserve path (src/lua/vm.c:1854-1881,:1731-1758,:1636-1641).
FINDINGS: none
PI_GAPS:
- Fallback still measures Indic-scope combining marks as 1 cell where pi gives standalone marks 0 via \p{Mark} (utils.ts:40,185) and host gives 0 (wcwidth.c:121 covers 0x08E3-0x0903; :122 starts Devanagari 0x093A) — e.g. U+0900..U+0903, U+093A; intentionally deferred to #220 per PR body.
- Fallback gives width 1 to default-ignorable/format chars pi zeroes via \p{Default_Ignorable_Code_Point} (utils.ts:40), host via nonprint table (wcwidth.c:69 U+00AD, :77 U+200B-0x200F) — soft hyphen, ZWSP/ZWNJ; pre-existing.
```

## Codex track (discovery)

```
PARITY_VERDICT: PARTIAL — Ordinary combining marks now follow pi-mono’s grapheme-width handling (pi-mono/packages/tui/src/utils.ts:39-43,174-208), and OSC 8 BEL/ST state is preserved across wraps as upstream requires (pi-mono/packages/tui/src/utils.ts:454-479,521-529,675-723). U+303F also leaves psi’s blanket wide range, matching pi-mono’s per-codepoint East Asian width path (pi-mono/packages/tui/src/utils.ts:194-208). However, pi-mono explicitly treats U+065F as a one-cell terminal-spacing mark (pi-mono/packages/tui/src/utils.ts:44-47,210-219), while this diff makes it zero-width.
FINDINGS:
- title: U+065F is incorrectly made zero-width
  path: lua/psi/tui_text.lua line: 157
  severity: should_fix
  category: bug
  reason_flagged: The newly expanded Arabic interval includes U+065F even though pi-mono explicitly classifies it as a terminal-spacing mark.
  body: The fallback now absorbs U+065F into its base without adding a cell, so width, clipping, and wrap boundaries undercount strings containing it; add a width-one spacing-mark cluster path for U+065F and align native/fallback coverage with pi-mono/packages/tui/src/utils.ts:44-47,210-219.
PI_GAPS:
- U+065F trailing a base remains one additional cell in pi-mono (pi-mono/packages/tui/src/utils.ts:210-219), but psi’s native table and newly aligned Lua fallback both count it as zero.
```

## Cross-validation record

```
FINDING 1: U+065F is incorrectly made zero-width — VALID — At f79946ae lua/psi/tui_text.lua:157 new range in_range(cp,0x064b,0x065f) puts U+065F in is_combining, codepoint_width (line 225) yields 0, next_cluster (:261-262) absorbs without adding a cell; pi-mono explicitly lists ٟ in terminalSpacingMarkRegex and adds a cell (utils.ts:47,216-218); pre-diff file had no Arabic ranges so U+065F was width 1 — regression introduced by diff. Severity should_fix correct: real width/clip/wrap undercount diverging from both pi-mono AND psi native table (src/core/wcwidth.c has no 0x06xx zero-width entries, returns 1 by fall-through), scoped to one rare codepoint on fallback-only path; parity test corpus omits U+065F so nothing catches it. (Codex's aside that the native table also counts it zero is wrong.)
```


## Limitations

- Static review only; CI (Lua suite, build matrix, PTY tests) treated as green per Forgejo Actions runs. No manual TUI interaction testing was performed.
- pi-mono parity claims cite upstream TypeScript sources read at review time (`c49906ec`); runtime Node/ICU behavior probed only where noted (#220).
- Diff scope: single commit vs origin/master.
